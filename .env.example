# No environment variables required by default.
# Ableton Live Assistant runs fully locally using OSC and MCP.
#
# Optional: OSC networking configuration (matches AbletonOSC defaults)
# Host where AbletonOSC is running (usually your local machine)
# ABLETON_OSC_HOST=127.0.0.1
# Port to SEND commands to AbletonOSC (AbletonOSC listens on 11000 by default)
# ABLETON_OSC_SEND_PORT=11000
# Port to RECEIVE replies from AbletonOSC (AbletonOSC replies from 11001 by default)
# ABLETON_OSC_RECV_PORT=11001
# Adjust the OSC response timeout (in milliseconds)
# Range: 500–15000 (default 5000)
# ABLETON_OSC_TIMEOUT_MS=3000
#
# If you add optional integrations in your own fork, define them here.
